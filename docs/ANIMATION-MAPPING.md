# Animation Mapping — Khosrow

Two layers are documented here:

1. **Clips** — the 11 physical animation rows on the sheet, with inferred
   identities and the evidence for each.
2. **State map** — how the 11 normalized Claude Code pet-states map onto those
   clips (a tunable UX decision).

Both are encoded authoritatively in
`app/Sources/KhosrowKit/Resources/khosrow.runtime.json` (generated by
`scripts/build_runtime_manifest.py`) and exercised by the Swift tests. This doc
is the human-readable rationale.

---

## Confidence legend

- **Measured** — comes directly from pixel analysis; not an opinion.
- **Inferred (high)** — a visual reading of the contact sheet that is hard to
  dispute (e.g. "arm raised").
- **Design** — a deliberate project choice that can be re-tuned without being
  "right" or "wrong".

| Aspect | Confidence |
|--------|-----------|
| Grid, cell size, frame counts, per-frame rects | **Measured** |
| Facing direction (front/left/right) | **Inferred (high)** |
| Clip *names* / semantic identity | **Inferred (high→medium)** |
| State → clip assignment, fps, dim | **Design** |

---

## 1. Clips (one per row)

| Clip | Row | Frames | Facing | fps | Loop | Visual evidence (from contact sheet) |
|------|-----|--------|--------|-----|------|--------------------------------------|
| `idle` | 0 | 7 | front | 6 | loop | Upright neutral stance, sword sheathed; frames nearly identical (0.2% change) → breathing idle. |
| `walk_left` | 1 | 8 | left | 12 | loop | Body angled left, legs alternating in a stride cycle. |
| `run_left` | 2 | 8 | left | 14 | loop | Same left facing but long, athletic strides — higher energy than row 1. |
| `cheer` | 3 | 4 | front | 12 | once | Right arm sweeps up and holds high — wave / salute. |
| `crouch` | 4 | 5 | front | 9 | once | Knees bend, figure sinks into a squat. |
| `bow` | 5 | 8 | front | 10 | once | Deep ceremonial bow: head + torso fold forward and down (bbox widens 67→182 px). |
| `present` | 6 | 6 | front | 10 | loop | Both arms open outward — presenting / explaining gesture. |
| `idle_guard` | 7 | 6 | front | 7 | loop | Front stance holding the sword, minimal motion — an alert standby idle. |
| `ready` | 8 | 6 | front | 10 | loop | Grips and manipulates the sword in front of the body — a "working" posture. |
| `walk_right` | 9 | 8 | right | 12 | loop | Thin side-profile facing right, walking; narrow silhouette. |
| `run_right` | 10 | 8 | right | 14 | loop | Side-profile facing right, faster; head dips forward. |

`once` clips play through and hold the final frame; `loop` clips repeat.

### Why these names are *inferred*

`pet.json` provides no animation names (see `docs/PET-JSON-SCHEMA.md`). The
`spriteVersionNumber: 2` format is expected to lay out **rows = clips** and the
host is expected to know the semantic order out-of-band. Rather than assert a
canonical order we cannot verify from the file, we **named each clip from what
its frames visibly show** and marked the confidence. If the upstream ChatGPT
ordering later becomes authoritative, only the `CLIPS`/`STATES` tables in
`scripts/build_runtime_manifest.py` need to change — no app code.

---

## 2. State map (Claude Code → clip)

The bridge (Phase 3) normalizes Claude Code activity into 11 states. Each maps
to a clip below. This is a **design** layer: edit the `STATES` table in
`scripts/build_runtime_manifest.py`, re-run it, and the app + tests pick up the
change.

| Claude state | Clip | Playback | Rationale |
|--------------|------|----------|-----------|
| `idle` | `idle` | loop 6 fps | Default resting stance. |
| `attentive` | `present` | loop 10 fps | Arms-open "I'm listening" when a session or sub-task starts. |
| `writing` | bundled † (`idle_guard`) | loop 6 fps | **Composing a response to your prompt** — reuses the `khosrow-reading-*` book frames until dedicated `khosrow-writing-*` art exists. |
| `reading` | bundled † (`idle_guard`) | loop 5 fps | **Hand-drawn `khosrow-reading-*` frames** — reads an open book (page-turn mid-cycle). |
| `searching` | `walk_right` | loop 12 fps | Moves/scans — searching the codebase. |
| `editing` | `ready` | loop 10 fps | Actively handling the sword = editing files. |
| `runningCommand` | `run_left` | loop 14 fps | Literally running = a command is executing. |
| `waitingForPermission` | `present` | loop 9 fps | Engaged, awaiting your input. |
| `success` | bundled † (`cheer`) | loop 9 fps | **Hand-drawn `khosrow-success-*` frames** — raises his sword in triumph. |
| `failure` | `bow` | once → hold 10 fps | Head-down bow reads as apology/defeat. |
| `sleeping` | bundled † (`idle`) | loop 4 fps | **Hand-drawn `khosrow-sleeping-*` frames** — sleeps in a drawn bed. |

† **Bundled-frame states.** `writing`, `reading`, `success`, and `sleeping` render
a hand-drawn transparent 192×208 frame sequence (`Resources/khosrow-<state>-*.png`,
played by `PetController.customAnims` on its own fps clock) instead of a sheet
clip. The clip in parentheses is the **fallback** used only if those frames are
missing from the bundle; its fps/loop in the manifest applies to that fallback.
`writing` has no dedicated art yet, so it reuses the `reading` book frames.

### Documented reuses / compromises

The sheet has 11 clips but no *dedicated* poses for a few states. Several now ship
**hand-drawn bundled frames** (see the † note above); the remaining compromises
reuse a clip or another state's art:

- **`waitingForPermission` reuses `present`** (shared with `attentive`). Both are
  "engaged, awaiting your input." A future build can add a distinct affordance
  (e.g. a "?" bubble) without new art.
- **`writing` reuses the `reading` book frames** until dedicated
  `khosrow-writing-*` art is added — both are book scenes, told apart by the mood
  label under the pet.
- **`reading` / `success` / `sleeping` use bundled frames**, not the sheet — a
  book-reading loop, a triumphant sword-raise, and a sleeping-in-bed scene
  respectively. Each falls back to a sheet clip (`idle_guard` / `cheer` / `idle`)
  only if its bundled PNGs are absent.

### Unbound bonus clips

`walk_left` (row 1) and `run_right` (row 10) are fully defined but not bound to a
Claude state. They are available to the **manual animation test mode** and to a
future "wander"/direction feature (e.g. face the direction of travel when the
pet is dragged).

---

## 3. Frame geometry (for renderers)

For clip on row `R` with `N` frames, frame `i` (0-based) is the pixel rectangle:

```
x = i * 192          (i in 0 … N-1)
y = R * 208
w = 192
h = 208
```

Draw the full 192 × 208 cell at a fixed **bottom-center** anchor; the feet sit at
y ≈ 202 within every cell, so no per-frame offset is needed. On Retina, treat the
sheet as `@2x` (each logical point = 2 device pixels) or apply the user scale —
see `docs/../ARCHITECTURE.md`.
